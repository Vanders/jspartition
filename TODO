o Add a "pre-OnAdd" callback so the user can enter a name
o Allow the user to Rename & Remove ranges
o A method to retrieve the range definitions as JSON
o The option to use Bootstrap (E.g. replace the Buttons & use Bootstrap CSS classes)
